udp通信模型:在通信开始之间，不需要建立相关的链接，只需要发送数据 （类似于写信）
udp不够安全
tcp:创建连接 数据传送 终止传输 （类似打电话）
tcp更加稳定
tcp为了保证不丢包，给每个包一个序号，同时序号也保证了传送到接收端实体的包
的按序接受，然后接收端实体对已成功收到的包发回一个相应的确认（ACK），如果
发送端实体在合理的往返时延（RTT）内未得到确认，那么相应的数据包就被假设为
已丢失将会进行重传。
服务端: 提供服务的一方
客户端：需要服务的一方
